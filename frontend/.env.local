PUBLIC_APP_PORT=5173
PUBLIC_VALIDATOR_BASE_URL=http://localhost:13000
# Must be reachable from the frontend server itself; use http://host.docker.internal:13000 when running in docker
PRIVATE_VALIDATOR_BASE_URL=http://host.docker.internal:13000
